---
description: List the connected wallet's identities and per-chain accounts
---

Call the `ac2_capabilities` tool and present what it returns: the wallet's
identities (DIDs / public keys) and its accounts grouped by chain (Algorand /
Base / Solana). Those are the real signing addresses — use them verbatim, never
guess or invent an address.
